export type { BlogArticleSeed } from '../2026-08-17-blog-articles/shared'

/**
 * Wave 3, failure-diagnosis cluster.
 *
 * Ten articles, one per failure mode, expanding the six modes that
 * `why-hydraulic-hoses-fail` names in four minutes. Each mode is its own
 * search, its own physical evidence and its own fix, and none of them fits in
 * a paragraph.
 *
 * SOURCING RULE FOR THIS CLUSTER
 *
 * These are diagnostic articles: the substance is what the evidence looks like
 * and what it implies, not numbers. Where a figure does appear it comes from
 * the same two places as the rest of the programme — HOSE_SIZE_TABLES for bend
 * radius and pressure, and ProductSpec for temperature ranges, cover materials
 * and tube compounds.
 *
 * Deliberately absent: percentage life-loss figures for twist, impulse cycle
 * counts, and abrasion test results. Those numbers circulate widely in this
 * industry with no attribution attached, and repeating them would make these
 * pages another link in that chain. Where a magnitude matters, the article
 * describes the mechanism instead.
 *
 * NO PHOTOGRAPHS YET. Every one of these wants a photograph of the actual
 * failure and none exists in the media library — the founder was asked to
 * start collecting them from the workshop. The articles are written so a
 * `figure` block drops in later without restructuring anything.
 */
