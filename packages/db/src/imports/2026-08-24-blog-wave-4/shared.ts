export type { BlogArticleSeed } from '../2026-08-17-blog-articles/shared'

/**
 * Wave 4 — machine and application cluster.
 *
 * Eleven articles extending the pattern the excavator, forklift and tipper
 * articles established in August: symptom identifies the circuit, the circuit
 * identifies a short list of hoses, and the assembly is specified without a
 * dealer part number.
 *
 * SOURCING RULE, INHERITED VERBATIM FROM THE EXCAVATOR ARTICLE
 *
 * Diagnostic, not a parts list. No model-specific part numbers, no dimensions,
 * no circuit pressures for named machines. Those come from the machine's own
 * parts book and its manufacturer's data, and inventing one would send someone
 * a hose that does not fit — or worse, a hose rated below the circuit.
 *
 * Where a figure appears it is about hose rather than about a machine, and it
 * comes from HOSE_SIZE_TABLES or ProductSpec like everything else in the
 * programme.
 *
 * WHAT THESE ARTICLES ARE FOR
 *
 * The reader is standing next to a stopped machine. Every one of them is
 * written to be useful in that position: what stopped, what that means, what
 * to photograph, what to send us. They are the lowest-competition, highest-
 * intent pages in the whole plan.
 */
