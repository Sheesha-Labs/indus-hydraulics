export type { BlogArticleSeed } from '../2026-08-17-blog-articles/shared'

/**
 * Wave 7 — items 11 to 20 of the Reddit-derived backlog.
 *
 * Ten articles, no new category. Where wave 6 took the highest-demand gaps,
 * this wave takes the ones that were either smaller clusters with unusually
 * high engagement per thread, or genuine gaps sitting next to an article that
 * already covers the neighbouring question.
 *
 * THE FOUR THAT SIT NEXT TO EXISTING ARTICLES
 *
 * Each of these was checked against the article it borders, and each is scoped
 * so the two do not overlap:
 *
 *   bspp-bonded-seal-sizing — the published BSPP vs BSPT article explains why a
 *   parallel thread needs a seal and stops there. This one is sizing.
 *
 *   where-jic-is-the-wrong-choice — the four-families comparison presents JIC
 *   neutrally. This takes a position, which is what earns a link.
 *
 *   field-re-hosing-kit — the published re-hosing article argues the case for
 *   replacing a set. This is the field execution of it.
 *
 *   mini-excavator-hose-maintenance — the inspection and replacement-programme
 *   articles cover the discipline. This narrows it to the machine class that
 *   dominates Gulf rental fleets.
 *
 * WHAT IS DELIBERATELY NOT IN THE COPY
 *
 * Bonded seal dimension tables, grease fitting size tables, and any hose grade
 * recommendation tied to a pressure figure. Same reasoning as wave 6: these are
 * per-part catalogue data, and a table published against a nominal size is the
 * kind of wrong that gets discovered on a machine. Each article gives the
 * identification method and sends the reader to the catalogue for the part in
 * their hand.
 *
 * The log splitter article deliberately refuses to name a hose grade. It
 * specifies the method — size against the relief setting, not the working
 * pressure — because the whole failure mode in that segment is people copying a
 * grade from a circuit that ran at a different relief setting.
 *
 * SOURCING
 *
 * ISO 228-1 and SAE J514 are cited as published documents and summarised in our
 * own words. Failure signatures, intervals and field method are our own
 * practice and are described as such. Topic selection came from the same Reddit
 * harvest as wave 6; no redditor's text is reused.
 */
