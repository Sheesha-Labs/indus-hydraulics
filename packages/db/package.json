{
  "name": "@indus/db",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "postinstall": "prisma generate",
    "db:generate": "prisma generate",
    "db:push": "node -e \"console.error('db:push is disabled on this project.\\n\\nPrisma refuses it here: the FTS tsvector column trips its data-loss guard, so the command cannot complete. Schema changes ship as hand-rolled SQL in packages/db/migrations/, applied through the Supabase MCP apply_migration tool.\\n'); process.exit(1)\"",
    "db:migrate": "prisma migrate dev",
    "db:seed": "tsx src/seed.ts",
    "db:import": "tsx src/import/cli.ts",
    "db:studio": "prisma studio",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@indus/domain": "workspace:^",
    "@prisma/client": "^6.19.3",
    "zod": "^4.4.1"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.105.1",
    "prisma": "^6.19.3",
    "tsx": "^4.21.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.2"
  }
}
